{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "test-chart.fullname" . }}
  labels:
    {{ include "test-chart.labels" . | nindent 4 }}
{{- end }}